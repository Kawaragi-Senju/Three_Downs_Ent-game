using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class сamera : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Я ебал всех в рот");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
